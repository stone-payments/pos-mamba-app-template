html,
body {
  height: 100%;
  width: 100%;
  display: flex;
  flex-direction: column;
}

header {
  background: #5fc569;
  box-shadow: inset 7px 20px 50px rgb(160 220 113 / 59%);
  /* background: linear-gradient(27deg, rgba(87, 186, 121, 1) 0%, rgba(160, 220, 113, 1) 100%); */
}

.modes {
  background: var(--seashell);
}
